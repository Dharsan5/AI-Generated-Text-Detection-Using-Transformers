# ============================================================
#  Hybrid Explainable Transformer Framework
#  AI-Generated Academic Text Detection
#  Requirements File
# ============================================================

# ── Core scientific stack ────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# ── Machine learning ─────────────────────────────────────────
scikit-learn>=1.3.0
xgboost>=2.0.0          # GPU (CUDA) support when built with CUDA

# ── Visualisation ────────────────────────────────────────────
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.14.0          # Interactive charts (HTML export)

# ── Persistence / serialisation ──────────────────────────────
joblib>=1.3.0

# ── Progress bars ────────────────────────────────────────────
tqdm>=4.65.0

# ── Standard library (no install needed) ─────────────────────
# argparse, logging, pathlib, re, html, string, typing,
# collections, concurrent.futures, subprocess, time, datetime

# --- Transformer / Deep Learning Stack ---

torch>=2.0.0

transformers>=4.30.0

datasets>=2.11.0

sentencepiece

accelerate>=0.20.0

# --- Explainability ---


peft
